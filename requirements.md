# 要件定義書｜Scenario Revenue Analyzer（仮称）

> LステップCSVと広告クエリCSVを投入するだけで、「どの広告流入が・どのシナリオを経て・どれだけ売上を生んだか」を非エンジニアでも判断できる、案件横断の小型DMPレポートエンジン。

- 文書バージョン: v1.2
- 作成日: 2026-06-10（v1.1: 2科確定・個人情報非保持・料金/シナリオ確定／v1.2 2026-06-11: STD単価・タグ構造確定・計上ルール確定・Google広告のみ＋API将来連携・LTV不要）
- 対象プロジェクト: **STD / dayvigo の2科のみ（bonjestaは対象外）**
- 元構想: `chapy.md`
- ステータス: ドラフト（主要事項確定・残未決1件）

> **個人情報ポリシー（最重要・先頭明記）**: 本システムは**氏名・電話・メール等の個人情報を一切保持・表示しない**。Lステップ友だちCSVから集計に必要な列（タグ・対応マーク・日時）のみをホワイトリストで取り込み、個人特定情報は読み込み時点で破棄する。Web上に個人情報が出る経路を構造的に排除する（詳細 §7.1）。

---

## 0. 今回の確定事項（方針決定）

| # | 論点 | 決定 | 含意 |
|---|------|------|------|
| 1 | 提供形態 | **Webアプリに統合** | 既存Next.js（listing-query-tool）を母体に、KPI生成・クエリ分析・統合レポートを1つのダッシュボードへ。CSVアップロード→分析→レポート出力までブラウザ完結 |
| 2 | 初期スコープ | **自社内DMPとして先行** | まず **STD / dayvigo の2案件**で使い込み、レポートの「型」を固める。マルチテナント・課金・認証は型が固まった後 |
| 3 | 売上データ | **Lステップ購入タグから推定** | 既存の購入タグ（デエビゴ定期/1/3/6ヶ月 等）× 商品単価で売上算出。新規データ連携は不要で即運用（= chapy.md の Phase 1） |
| 4 | 対象診療科 | **STD / dayvigo の2科のみ** | bonjesta（つわり）は今回スコープ外 |
| 5 | 個人情報 | **一切保持・表示しない** | ファネル（決済→問診票提出→ビデオ診察→発送）とタグCTA・最終アクション日の集計のみ。氏名/電話/メールは取り込まない |

---

## 1. 背景と目的

### 1.1 背景
onemedical（CUREAオンライン外来）の運営において、Claude Code 上で蓄積してきた以下のノウハウが、すでに**2つの稼働中ツール**として存在する。

1. **Lステップ分析** — `dayvigo/data/KPI表_20260609.xlsx`
   Lステップの「友だち情報（状況）CSV」を生データシートに貼り付けると、STEP0〜6のタグ進捗・購入タグから、月別KPI比較／対応マーク分布／ファネル分析／定期率分析／週次ログを自動生成する。
2. **広告クエリ分析** — `~/Work/tool/listing-query-tool`（Next.js）
   Google広告の検索語句CSVをアップロードし、期間比較で各クエリを「勝ち/悪化/無駄/機会損失/新規/消滅/監視」に自動分類し、優先度スコアと推奨アクションを出力する。

しかし両者は**分断**しており、「広告クエリ → LINE登録 → シナリオ反応 → CV → 売上」という一連の流れで成果を診断できていない。また案件ごとに手作業が発生し、外注に渡しにくい。

### 1.2 目的
- 広告・Lステップ・売上を**1つの売上貢献レポート**に統合し、「止めるもの・伸ばすもの・作り直すもの」を即判断できる状態を作る。
- 案件横断のノウハウを**スコア化・テンプレ化**し、CSV投入だけで誰でもレポート初稿を作れるようにする（外注化の土台）。
- 将来的にLステップ構築者・広告代理店向けSaaSへ横展開できる設計にする。

### 1.3 非目的（今回やらないこと）
- 完全なgclid/utmベースの広告アトリビューション（Phase 3 で対応）
- マルチテナント認証・課金・チーム権限（自社内DMPの型が固まるまで保留）
- Lステップ/Google Ads APIの自動取得（当面は手動CSVアップロード）

---

## 2. 既存資産と本システムの差分（最重要）

本システムは**ゼロから作るのではなく、既存2資産を統合レイヤーで束ねてプロダクト化**するもの。

| 領域 | 既存資産 | 本システムでの扱い |
|------|----------|------------------|
| Lステップ分析 | KPI Excel（月別/マーク/ファネル/定期率/週次） | Webへ移植。生データ34カラムのタグ集計ロジックを再実装し、シナリオスコアを追加 |
| 広告クエリ分析 | listing-query-tool（分類7種＋優先度スコア） | そのまま母体として採用。RSA分析を追加 |
| **統合分析** | **なし（＝本システムの新規価値）** | 広告クエリ → 流入経路 → シナリオ反応 → 購入タグ → 売上 を接続 |
| 売上推定 | KPIの購入タグ集計のみ | 購入タグ × 商品単価マスタで売上額を算出 |
| レポート | 手作業 | テンプレからMarkdown/HTML自動生成 |
| プロジェクト管理 | ファイル分散 | 案件ごとに設定・履歴を管理 |

> **本システムの1000万ドル価値は「統合分析」にある。** 広告単体・Lステップ単体で見ず、流入からCV・売上まで一気通貫で診断する。

---

## 3. プロダクトコンセプト

**LステップCSVと広告CSVをアップロードするだけで、売上を生むシナリオと無駄な広告を自動診断するレポートツール。**

本質的な問い:
> どの広告流入が、どのLINEシナリオに入り、どの配信・タグ・クリック行動を経て、最終的に売上価値を生んだのかを、**非エンジニアでも判断できる状態**にすること。

---

## 4. 想定ユーザーと提供価値

| ユーザー | ニーズ | 提供価値 |
|----------|--------|----------|
| 森本さん本人 | 複数案件のLINE・広告改善を短時間で判断 | CSV投入→数分で全案件の診断レポート |
| 運用メンバー/外注（伊藤さん等） | CSVを入れるだけでレポート初稿 | マッピング設定済みなら投入のみで完成 |
| 広告運用者 | クエリ・RSA・広告文の改善点 | 除外候補・強化候補を自動抽出 |
| （将来）クライアント | 何が成果に効いているか簡単に理解 | クライアント向け要約レポート |

---

## 5. スコープとフェーズ

### 5.1 MVP（最優先・自社内DMP）
chapy.md の5機能 ＋ 既存資産の統合。

| 優先 | 機能 | 内容 |
|------|------|------|
| 1 | CSVアップロード | Lステップ友だちCSV・広告クエリCSV をブラウザ投入 |
| 2 | データ正規化／列マッピング | 列名ゆれ・日付形式・タグ名ゆれを吸収（案件別設定） |
| 3 | Lステップ分析 | KPI／ファネル（STEP0〜6）／シナリオスコア／売上推定 |
| 4 | 広告クエリ分析 | 既存7分類＋優先度スコア（移植）＋RSA分析 |
| 5 | 統合分析 | 流入経路 × シナリオ × CV × 売上の接続表 |
| 6 | 自動レポート生成 | Markdown/HTML（内部用・クライアント用） |

### 5.2 アトリビューション3段階（盲点対応）
LステップCSVだけでは広告クエリ単位の完全な売上貢献は追えない（登録後にgclid/utmが消える）。

| 段階 | 接続キー | 本システムでの位置づけ |
|------|----------|----------------------|
| **Phase 1** | 流入経路・登録日時・タグの近似分析 | **今回のMVP**。売上は購入タグ×単価で推定 |
| Phase 2 | URLパラメータ・登録経路・タグで接続 | 次フェーズ |
| Phase 3 | gclid・電話番号・メール・決済IDで高精度連携 | 将来（決済データ連携と同時） |

### 5.3 将来スコープ
マルチテナント／認証／課金／Google Ads API・LステップAPI自動取得／BigQuery連携／Google Docs出力。
→ 想定マネタイズ: 「LINE構築後の改善レポートを月額5万〜15万円で提供する診断ツール」。

---

## 6. 機能要件

### F1. プロジェクト管理
- 案件（STD / dayvigo）ごとに、設定ファイル・アップロード履歴・レポート履歴を管理。
- 案件を切り替えるだけで、列マッピング・CVタグ・商品単価・スコア重みが切り替わる。

### F2. データ取込
- **Lステップ友だちCSV**: Lステップ「友だち情報（状況）」エクスポート。1行＝1友だち、タグ列はフラグ（`1`/空）。
  - 主要カラム例（実データ34列）: `ID`／`表示名`／`対応マーク`／`友だち追加日時`／`RM_決済クリック（単品/定期）`／`診察誘導_STEP0A/0B/1〜6`／`STEP0_薬使用中/未使用`／`デエビゴ定期/1/3/6ヶ月`／`申込経由STEP`／`最終アクション日`。
- **広告クエリCSV**: **Google広告のみ（確定）**。Yahoo/Meta等は対象外。検索語句レポート（`日付`/`検索語句`/`表示回数`/`クリック数`/`費用`/`コンバージョン` ＋任意で `キャンペーン`/`広告グループ`/`キーワード`/`CV値`）。
- **（将来）RSA CSV**: 広告見出し・説明文アセット別レポート。
- 文字コード自動判定（Shift_JIS/UTF-8、`encoding-japanese`）。費用のカンマ区切り（`"15,000"`）を吸収。

### F3. 正規化・列マッピング
- `config/<project>/column_mapping.yaml` でCSV列名→標準項目へマッピング（列名ゆれ対応）。
- 日付形式統一、タグ名の表記ゆれ吸収、対応マークの正規化。
- マッピング未設定時はUI上で列を標準項目に紐付ける画面を提供。

### F4. Lステップ分析（既存KPI Excel相当＋拡張）
既存KPIシートのロジックを移植・拡張する。
- **月別KPI比較**: 新規LINE登録数・決済完了数（定期/単品1/3/6ヶ月）・CVR・定期率・前月比。新旧フロー区別（移行日 2026-04-29）。
- **対応マーク分布**: 未対応／発送済／キャンセル／決済案内済み／昨日決済(問診票未提出) 等の人数・構成比。**「決済済み・問診票未提出」の要フォロー層を明示**（新フローの最重要ボトルネック）。
- **ファネル分析（新フロー・売上ファネル）**: 新フロー `LINE登録 → 決済 → 問診票提出 → ビデオ診察(BellFace) → 発送` を主軸に集計する。
  - ① LINE登録（集計起点）
  - ② 診察スタート押下（STEP0A+0B）／③ STEP1到達 … LINE内エンゲージメント
  - ④ 決済クリック（RM_決済クリック 定期/単品）→ ⑤ 決済完了（購入タグ＝決済済み）
  - ⑥ 問診票提出済み → ⑦ 診察完了 → ⑧ 発送済（対応マーク）
  - 各段の転換率・目標・判定。**決済済みかつ問診票未提出の要フォロー層を必ず明示**（新フロー最大のボトルネック）。
  - ※⑥⑦はタグ「問診票提出済み」「診察完了」が付与されている前提（onemedical CLAUDE.md のタグ設計）。タグ未設置期間は欠損として注記。
- **定期率分析**: 旧フロー vs 新フローの定期率推移と単品内訳の変化。
- **週次ログ**: 週（月曜起点）ごとの登録数・決済数・定期率・ボトルネック。
- **シナリオ価値スコア（新規）**: シナリオ単位で 到達数／クリック率（タグCTA押下）／CV率／売上額 を評価。対象シナリオは案件別に固定で持つ（`config/<project>/scenarios.yaml`）。
  - **dayvigo**:
    - `新フロー対応 友だち追加シナリオ（2026/05/11）_登録直後・15分後`
    - `新フロー対応 友だち追加シナリオ（2026/05/11）_時刻21時指定`
  - **STD**:
    - `【登録時】新フロー対応 友だち追加シナリオ（2026/05/27）_登録直後・15分後`
    - `新フロー対応 友だち追加シナリオ（2026/05/27）_時刻指定21時`
  - ※開封率・ブロック率は**取得しない**（個人情報非保持方針・最低限集計方針）。到達・タグCTA押下・CV・売上で評価し、欠損指標はレポートに「未計測」と明示。
- **STEP別 申込貢献分析（新規・タグ設計に準拠）**: `診察誘導_STEP○○` タグ × `決済済み` で「どのSTEPのCTAが申込に最も貢献したか」を数値化する。
  - STDタグ体系（出典 `STD/docs/タグ命名規則_友だち追加シナリオv1.md`）:
    - STEP0分岐: `STEP0_治療希望`（申込フローへ）／`STEP0_検査希望`（検査キットシナリオへ・STEP1〜6から除外）
    - CTA貢献（**追記式**＝複数STEPタップで複数付与）: `診察誘導_STEP0A`／`STEP1`〜`STEP6`／`FAQ1`／`FAQ2`
    - 再アプローチ: `シナ読了_対象`（STEP6到達かつ未決済）
    - ラストタッチ: 友だち情報欄 `申込経由STEP`（上書き＝最後にタップしたSTEP）
  - 分析: `診察誘導_STEPn（成約）= 診察誘導_STEPn AND 決済済み` の人数で各STEPの申込貢献を比較。**ラストタッチ（申込経由STEP）と全タッチ（診察誘導タグ追記）の両面**で集計可能。
  - dayvigo も同体系（生データに `診察誘導_STEP0A/0B/1〜6`・`申込経由STEP`・`最終アクション日` を保持）。
  - ※`最終アクション日`・`申込経由STEP` は個人を特定しない集計値として利用（再エンゲージ対象数・STEP分布の把握）。個人単位リストは出さない（§7.1）。

### F5. 広告クエリ分析（既存listing-query-tool相当）
- 期間比較（当期 vs 前期）で各クエリを分類: **勝ち／悪化／無駄／機会損失／新規／消滅／監視**。
- 母数不足フィルタ（クリック<10・費用<3000・表示<100 等は監視へ）。
- **保護ワード（要確認）**: ブランド名・高意図ワード（例: デエビゴ／指名）を含むクエリは「無駄/悪化」でも除外前に要確認フラグ。
- 優先度スコア（CV減少インパクト＋費用悪化＋CPA悪化＋機会損失）で並べ替え。
- 集計指標: 表示回数・クリック・費用・CV・CPA・ROAS・CTR・CVR。
- **RSA分析（新規）**: 見出し/説明文別の表示・CTR・CVR傾向、勝ち訴求・釣り訴求・露出不足の判定。
- **広告ソースはGoogle広告のみ（確定）**。

#### F5-2. Google Ads API / MCP連携（将来拡張・出典 `GoogleadAPI.md`）
手動CSVアップロードから、Google Ads API（MCPサーバー経由）での自動運用へ拡張する道筋を設計に織り込む。

- **自動取得**: 検索語句レポートをAPIで日次取得（CSV手動アップロード不要化）。
- **自動仕分け提案**: 情報収集クエリ（「とは」「使い方」系）→ 除外KW候補／高意図クエリ（「料金」「比較」「どこ」系）→ 新規KW候補 を自動抽出。
- **入札・除外の適用**: 除外KW追加・新規KW追加・入札調整を提案→**目視確認のうえ適用**（自動適用は最初は必ず人間が確認）。
- **日次スケジュール実行**: 「データ見て最適化」を定期実行で回す。
- 構成: Google Ads API → MCPサーバー（google-ads-python ラップ）→ Claude Code。
- 前提: 開発者トークン審査・OAuth2（refresh token）・`google-ads.yaml`・Python環境が必要。**MVPスコープ外**（型が固まった後）。

### F6. 統合分析（本システムの核・新規）
広告クエリ → LINE流入 → 反応シナリオ → CV率 → 売上 を1表で接続。

| 広告クエリ | LINE流入 | 反応シナリオ | CV率 | 売上 | 判断 |
|-----------|---------|------------|------|------|------|
| 睡眠薬 オンライン | 120 | 初回不安解消 | 12% | 180,000 | 強化 |
| 睡眠薬 通販 | 300 | 価格訴求 | 2% | 80,000 | 除外検討 |

- 接続は Phase 1 では「流入経路 × 登録日 × タグ」の近似マッチ。精度限界を明記。

### F7. 売上推定（タグ×単価）
- `config/<project>/product_pricing.yaml` に商品単価マスタを持つ。購入タグ付与人数 × 単価 で売上を算出。
- 定期は初回単価ベースで計上（LTV/2回目以降は将来拡張）。

**dayvigo 単価マスタ（確定・税込）** — 出典 `dayvigo/docs/料金比較_単品料金一覧.md`

| 商品（購入タグ） | 内訳 | 計上単価（税込） |
|------------------|------|----------------|
| デエビゴ定期（3ヶ月定期 初回） | 診察料・送料・システム利用費 無料 | **2,980円**（初回）／2回目以降 9,680円 |
| デエビゴ1ヶ月（単品） | 薬代7,480 ＋ システム1,650 ＋ 郵送550 | **9,680円** |
| デエビゴ3ヶ月（単品・90日一括/10%OFF） | (薬代6,732×3) ＋ システム1,650 ＋ 郵送550 | **22,396円（一括計上・確定）** |
| デエビゴ6ヶ月（単品・180日一括/15%OFF） | (薬代6,358×6) ＋ システム1,650 ＋ 郵送550 | **40,348円（一括計上・確定）** |

> **計上ルール（確定）**: 単品3/6ヶ月は**一括計上**（月割しない）。定期は**初回2,980円**でCV売上に計上。
> **LTVは扱わない（確定）**。MVPは初回/一括の実売上のみを集計。LTV参考値（=実装対象外）: 単品含む現状LTV約56,000円（F6回×単価9,000円）、目標 定期客単価30,000円・単品客単価10,000円。

**STD 単価マスタ（確定・税込）** — 出典 `STD/docs/料金.md`

| 商品（購入タグ） | 販売単価（税込） | 原価(参考) |
|------------------|----------------|-----------|
| 性病治療薬（ジスロマック等：クラミジア/ウレアプラズマ） | **8,140円** | 1,034 |
| 女性10項目／フルセット | **28,000円** | 8,309 |
| 女性8項目／スタンダード | **19,000円** | 6,527 |
| 女性6項目／ベーシック | **15,000円** | 5,367 |
| 女性2項目／ライト | **9,900円** | 4,049 |
| 男性10項目／フルセット | **28,000円** | 8,309 |
| 男性7項目／スタンダード | **19,000円** | 6,587 |
| 男性5項目／ベーシック | **15,000円** | 5,367 |
| 男性2項目／ライト | **9,900円** | 4,049 |

> 原価も判明しているため、**売上だけでなく粗利（売価−原価−送料310−梱包20−決済手数料2.65%）も算出可能**。粗利分析は将来拡張で対応。

- クーポン適用: **dayvigo `SLEEP500`／STD `STD2026`（20%OFF）**。タグで適用ユーザーを識別できる場合のみ売上に反映（将来対応）。MVPは定価ベース。

### F8. レポート生成
- テンプレ: **内部用**（詳細）／**クライアント用**（要約）の2種。
- 出力: Markdown（必須）→ HTML（必須）→ PDF/Google Docs（将来）。
- AIコメント生成（任意・後付け）: 数値根拠から結論・改善提案文を生成。
- レポート構成: 結論 / シナリオ分析 / 広告クエリ分析 / RSA分析 / 統合示唆 / 今すぐ止める / 今すぐ伸ばす / 次回テスト案 / クライアント向け要約。

### F9. 設定
- CVタグ・売上タグ・除外タグ・保護ワード・商品単価・スコア重みを案件別に設定。
- スコア重みは案件特性で変更可能（オンライン診療＝CV率重視 等）。

---

## 7. 非機能要件

### 7.1 個人情報の非保持（最優先・確定方針）
**Web上に個人情報が一切出ないことを構造的に保証する。** 「個人情報を見に行かなくても、ファネルとタグCTA・最終アクション日の集計ができれば十分」という方針に従う。

- **取り込みホワイトリスト方式**: Lステップ友だちCSVから、集計に必要な列**のみ**を取り込む。
  - 取り込む列: `ID`（※後述の通り集計キーのみ・表示しない）／`対応マーク`／`友だち追加日時`／各 `診察誘導_STEP*` タグ／`RM_決済クリック（単品/定期）`／`STEP0_薬使用中/未使用`／購入タグ（`デエビゴ定期/1/3/6ヶ月` 等）／`問診票提出済み`・`診察完了` タグ／`申込経由STEP`／`最終アクション日`。
  - **取り込まない列（即破棄）**: `表示名`、その他 氏名・電話番号・メール・住所等の個人特定情報。CSVに存在しても読み込み時点でドロップする。
- **個人単位の行を出さない**: 画面・レポート・出力ファイルには、個人を識別できる行（1人1行のリスト）を一切表示しない。出力は**集計値・分布・ファネル・スコアのみ**。
- **IDの扱い**: `ID` はファネル内の重複排除・タグ突合のための集計キーとしてのみメモリ上で使用し、画面・レポート・保存物に出さない。永続化する場合はハッシュ化（or そもそも保存しない）。
- **非永続（ブラウザ内処理）**: MVPはアップロードCSVをサーバ/DBに保存せず、ブラウザ内で集計して結果のみを扱う。生データはサーバに送らない。
- **出力レポートにPIIを含めない**: Markdown/HTML出力は集計・示唆のみ。第三者（クライアント）に渡しても個人情報が漏れない状態を保証。

### 7.2 その他
- **非エンジニア運用**: CSVアップロードと案件選択だけで完結。設定はYAML/UIどちらでも。
- **再利用性**: 案件追加は設定ファイル追加のみ。コード変更不要。
- **データ量**: Lステップ生データは数万行（実績 32,000行超）。クライアント側集計で耐える設計、必要に応じてサーバ集計（その場合もPIIは送らない）。
- **再現性**: 同一CSV・同一設定なら同一レポート。スコアロジックはテスト可能（既存vitest踏襲）。

---

## 8. データモデル（論理）

> **注**: `display_name` 等のPIIはモデルに**持たない**（§7.1）。`user_id` は集計キーのみ・非表示・永続化しない。

```
projects        : id, name(std/dayvigo), flow_type, created_at
lstep_friends   : project_id, user_id(集計キーのみ・非表示), mark, added_at,
                  scenario_step(STEP0A..6), payment_click_flags,
                  purchase_tags(定期/1/3/6ヶ月),
                  shinsatsu_done, monshin_submitted, last_action_at
ad_queries      : project_id, date, campaign, ad_group, search_query,
                  impressions, clicks, cost, conversions, cv_value
rsa_assets      : project_id, date, headline/description, impressions, clicks, conversions
product_pricing : project_id, product_name, unit_price
scenarios       : project_id, scenario_name（評価対象・案件別固定）
conversions     : project_id, conversion_date, conversion_type, product, revenue(推定)  # 個人単位の保存はしない・集計のみ
```

> MVP（自社内DMP）ではDBを持たず、ブラウザ内集計＋結果のみ（生データ・PIIは非永続）。マルチテナント化時に Supabase PostgreSQL へ移行（その際もPIIは保存しない設計を維持）。

---

## 9. 分析ロジック / スコアリング

### 9.1 シナリオスコア
```
Scenario Score =  CV率 × 35
                + クリック率 × 20
                + 売上貢献率 × 25
                + CV速度スコア × 10
                - ブロック率 × 20
```

### 9.2 クエリスコア
```
Query Score =  CV数 × 20
             + ROAS × 25
             + CTR × 10
             - CPA悪化度 × 20
             - 無駄クリック率 × 15
```
- 重みは案件別に変更可能（`scoring_rules.yaml`）。
- 既存 listing-query-tool の `calcPriorityScore`（CV減少・費用悪化・CPA悪化・機会損失インパクト）を分類用シグナルとして併用。

### 9.3 判定ルール（ルールベース改善提案）
| 状態 | 判断 |
|------|------|
| 費用高・CVなし | 除外候補 |
| CTR高・CV低 | 訴求とLPのズレ |
| CV高・CPA低 | 強化候補 |
| 表示多・CTR低 | 広告文改善 |
| シナリオ: CV率高・ブロック低・売上高 | 拡張対象 |
| シナリオ: 開封高・クリック低 | CTA改善 |
| シナリオ: クリック高・CV低 | 遷移先LP改善 |
| シナリオ: ブロック高 | 配信停止/文面修正 |

---

## 10. 画面要件

| 画面 | 内容 |
|------|------|
| プロジェクト一覧 | 案件ごとの分析履歴・最終更新 |
| CSVアップロード | Lステップ／広告（／将来 RSA・売上）を投入 |
| マッピング画面 | CSV列→標準項目の紐付け |
| Lステップ分析 | KPI・マーク分布・ファネル・定期率・シナリオスコア |
| 広告クエリ分析 | 分類タグ別テーブル・優先度順・除外/強化アクションリスト |
| 統合分析 | 広告→LINE→CV→売上 の接続表 |
| レポート生成 | 内部用/クライアント用を選択しMarkdown/HTML出力 |
| 設定 | CVタグ・単価・スコア重み・保護ワード |

---

## 11. 技術構成

| 項目 | 採用 | 備考 |
|------|------|------|
| フロント/フレームワーク | **Next.js（既存listing-query-tool母体）** | App Router・React 19・Tailwind |
| CSVパース | papaparse ＋ encoding-japanese | 既存流用 |
| 分析処理 | TypeScript（クライアント集計） | 既存ロジック流用。重い集計はサーバへ切出し可 |
| グラフ | recharts | 既存流用 |
| テスト | vitest | 既存流用（scoring/classify にテスト有） |
| レポート出力 | Markdown→HTML | PDF/Docsは将来 |
| AIコメント | Claude API（任意・後付け） | 数値根拠からの文章生成 |
| デプロイ | Vercel | 既存 .vercel 有 |
| （将来）DB/保存 | Supabase PostgreSQL / Storage | マルチテナント化時 |

> 注: chapy.md は Python/FastAPI/Supabase を将来構成として挙げているが、**確定した「Webアプリ統合」方針では、稼働中の Next.js を母体に拡張する**のが最短。重い集計が必要になった時点でサーバ関数（または Python マイクロサービス）を切り出す。

---

## 12. ディレクトリ構成案（Next.js母体・拡張）

```
revenue-analyzer/                # 既存 listing-query-tool を母体に拡張
  config/
    <project>/                   # 案件別（std / dayvigo の2科）
      column_mapping.yaml
      product_pricing.yaml
      scoring_rules.yaml
      cv_tags.yaml
      scenarios.yaml             # 評価対象シナリオ名（案件別固定）
  src/
    app/                         # 画面（一覧/アップロード/分析/レポート/設定）
    lib/
      ingest/                    # CSV読み込み・文字コード
      normalize/                 # 列マッピング・タグ正規化
      lstep/                     # KPI・ファネル・シナリオスコア（KPI Excel移植）
      ads/                       # classifyQueries・scoring（既存流用）・RSA
      integrate/                 # 統合分析（広告×LINE×売上）
      revenue/                   # 売上推定（タグ×単価）
      report/                    # Markdown/HTML生成・テンプレ
    components/                  # テーブル・カード・グラフ（既存流用）
    test/                        # vitest
  output/reports/                # 生成レポート（内部運用時）
```

---

## 13. 受け入れ基準

| 要件 | 合格条件 |
|------|----------|
| CSV取込 | Lステップ友だちCSV・広告クエリCSVを読み込める（Shift_JIS/UTF-8・カンマ数値対応） |
| 列名ゆれ対応 | 案件別 column_mapping で列名を変更できる |
| Lステップ分析 | 既存KPI Excel と同等の月別KPI・マーク分布・ファネル・定期率を再現できる |
| シナリオ分析 | シナリオ別CV率・売上貢献・スコアを出せる |
| 広告分析 | クエリ別CPA・ROAS・除外/強化候補を出せる（既存7分類を再現） |
| 統合分析 | 流入経路×シナリオ×CV×売上の接続表を出せる |
| 売上推定 | 購入タグ×単価で売上額を算出できる |
| レポート生成 | 内部用・クライアント用のMarkdownを出せる |
| 再利用性 | 案件追加が設定ファイルのみで完結する |

---

## 14. 盲点・リスク

| 項目 | リスク | 対応 |
|------|--------|------|
| アトリビューション限界 | 登録後にgclid/utmが消え、クエリ→売上の完全追跡は不可 | Phase制（近似→パラメータ→決済ID）。MVPは近似と明記 |
| 指標欠損 | 開封率・ブロック率がLステップエクスポートに含まれない場合がある | 欠損指標はスコアから除外し、レポートに「未計測」と明示 |
| 個人情報 | Lステップ生データに氏名等を含む | **取り込みホワイトリストでPIIを即破棄・非保持・非表示**（§7.1で確定） |
| タグ設置時期 | 決済クリックタグは2026-05-27〜計測開始など、期間で欠損 | 集計期間とタグ有効期間を突合し注記を自動付与 |
| 新旧フロー混在 | 移行日(2026-04-29)前後でファネル定義が異なる | フロー種別で集計を分離（既存KPI踏襲） |
| 売上の精度 | 単価固定・LTV未考慮・クーポン未反映 | Phase 1は推定値と明示。決済データ連携で高精度化 |

---

## 15. ロードマップ

| マイルストン | 内容 | 主成果物 |
|------|------|----------|
| M0（本書） | 要件定義 | requirements.md |
| M1 | データ取込・正規化・Lステップ分析移植 | KPI/ファネル/定期率をWebで再現（dayvigoで検証） |
| M2 | 広告クエリ分析統合＋RSA | 既存分類＋RSAをダッシュボードへ |
| M3 | 統合分析＋売上推定 | 広告×LINE×売上 接続表 |
| M4 | レポート自動生成（内部/クライアント） | Markdown/HTML出力 |
| M5 | STD展開（設定追加のみ） | STD/dayvigo 2案件運用 |
| M6（将来） | Google Ads API/MCP連携（自動取得・KW仕分け・入札・日次実行） | 広告運用自動化（§F5-2） |
| M7（将来） | マルチテナント/認証/課金 | SaaS化 |

---

## 16. 未決事項（次に詰める）

### 残課題
1. **STD 商品レベルの売上識別**: STDは購入を共通タグ `決済済み` で判定するが、タグ命名規則上**どの商品（治療薬/各検査キット）を買ったかを示すタグが未定義**。商品別売上を出すには商品識別タグ or 別データが必要。
   - 暫定案: MVPは `STEP0_治療希望`→治療薬8,140円 を基本線とし、検査キット（`STEP0_検査希望`）は別シナリオ管理のため商品別売上は将来対応。あるいは決済済み×平均単価で概算。→ **要方針決定**。
2. **広告データの粒度**: 検索語句のみか、キャンペーン/広告グループ/RSAアセット別レポートも入手可能か（Google広告で確定）。
3. **クーポン反映**: `SLEEP500`/`STD2026`(20%OFF) を売上推定にどう反映するか（タグで適用者を識別可能か）。
4. **既存KPI Excelの計算式**: Web移植時に再現すべき自動計算ロジックの分母定義（特に定期率・各転換率）。
5. **rinaさん調査待ち**: 現在 rina さんに別途調査を依頼中。結果が出たら本書へ反映する。

### 解決済み（v1.1で確定）
- ~~対象案件~~ → **STD / dayvigo の2科**（bonjesta除外）。
- ~~個人情報の取り扱い~~ → **非保持・非表示**（§7.1）。集計に必要な列のみホワイトリスト取り込み、PIIは即破棄。
- ~~シナリオ定義~~ → **案件別に評価対象シナリオ名を固定**（§F4・`scenarios.yaml`）。
- ~~開封率・ブロック率~~ → **取得しない**（最低限集計方針）。スコアは到達・タグCTA・CV・売上で算出。
- ~~dayvigo単価~~ → **確定**（§F7）。
- ~~STD販売単価~~ → **確定**（§F7。治療薬8,140円／検査キット 9,900〜28,000円。原価も判明＝粗利算出可）。
- ~~dayvigo 3/6ヶ月単品の計上~~ → **一括計上で確定**（§F7）。
- ~~定期/LTV計上~~ → **LTV不要・初回単価で計上**（§F7）。
- ~~問診票提出済み/診察完了タグの有無~~ → **共通フロータグとして実在確認**（タグ命名規則v1）。ファネル⑥⑦に使用可。
- ~~STDタグ構造~~ → **確定**（§F4。`STEP0_治療希望/検査希望`・`診察誘導_STEP0A/1-6/FAQ1/FAQ2`・`シナ読了_対象`・`申込経由STEP`）。
- ~~広告ソース~~ → **Google広告のみ**（§F2）。将来 Google Ads API/MCP連携（§F5-2）。

---

## 17. 拡張機能: 検索語句単位 実CV突合CPA分析（ECP/STD・Phase 0）

`0813renewal.md`（大量CR・大量LP・大量媒体を実CVベースで管理する広告運用ツールの作り方）の方法論を、ECP/STDの2案件に適用する検討を開始した。既存の§F5「広告クエリ分析」は**媒体（Google Ads）が自己計測したコンバージョン**で検索語句を分類しているが、実際の成約・売上（実CV）と一致するとは限らない。本機能はgclid等の突合キーで媒体RAWDATAと実CV RAWDATAを接続し、**検索語句／キーワード単位**の実CPAを可視化する、既存F5の拡張。

- 詳細仕様: `spec/検索語句単位実CPA分析_要件v1.md`
- 位置づけ: §5.2「アトリビューション3段階」の **Phase 3（gclid・電話番号・メール・決済IDで高精度連携）を具体化するもの**。0813renewal.mdの元記事はMeta等の画像クリエイティブ単位を想定するが、本プロジェクトの広告媒体はGoogle広告のみ（§F5で確定済み）のため、粒度を検索語句／キーワードに置き換えて設計している。
- 現状（2026-08-13）: **前提となる突合キー（gclidのオフラインコンバージョンインポート運用、またはValueTrackパラメータ設定）・実CV側のURL/gclid記録が未整備**。着手にはクライアント（伊藤さん等）への確認・データ整備依頼が先決のため、実装は未着手（Phase 0の要件整理のみ完了）。

---

## 付録0. 実装ハンドオフ（Codex向け）
- **詳細仕様書**: `spec/詳細仕様_v1.md` — データモデル・取込・分析ロジック・ファネル判定（案件別）・スコア式・レポート・モジュール構成・受入基準・実装順を網羅。
- **確定config**: `config/dayvigo/config.yaml` / `config/std/config.yaml` — 列マッピング・タグ・単価・シナリオ・ステータス導出を確定値で格納。Codexはハードコードせず本configを読む。
- 実コーディングは **Codex** が担当（本要件＋詳細仕様＋configを単一参照とする）。

## 付録A. 参照資産
- 構想: `chapy.md`
- 検索語句単位実CPA分析の方法論: `0813renewal.md`（§17参照）
- Lステップ分析（移植元）: `dayvigo/data/KPI表_20260609.xlsx`（シート: 月別KPI/マーク分布/ファネル/定期率/週次ログ/生データ34列）
- 広告クエリ分析（母体）: `~/Work/tool/listing-query-tool`（`src/lib/classifyQueries.ts`・`scoring.ts`・`aggregateQueries.ts`・`comparePeriods.ts`）
- 新フロー/タグ設計/ブランド規定: `onemedical/CLAUDE.md`

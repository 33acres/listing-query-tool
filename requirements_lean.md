# 要件書｜STD Weekly Revenue Analyzer Lite

## 1. 目的
毎週月曜日に、STD（CUREAオンライン性病外来）のGoogle Drive指定フォルダへ置かれたLステップCSV・広告CSV・全体管理CSV/xlsxをまとめて取り込み、先週どこがボトルネックだったかを説明できる状態にする。

このツールは、毎日の細かい監視ではなく、週次定例で使う説明材料を一気に作るための軽量分析ツールである。

dayvigoは今回の要件から外す。以後、この要件書で対象とする案件はSTDのみ。

## 2. 変えない前提
- 広告CSVとLステップCSVはSTDのGoogle Drive指定フォルダから読み取る
- 全体管理CSV/xlsxもSTDのGoogle Drive指定フォルダから読み取る
- Google Driveから取得したファイルはブラウザ内で処理し、ツール側サーバ/DBに保存しない
- 分析結果は個人情報を含まない集計値だけをSTDのGoogle Drive指定フォルダへ書き戻す
- 個人情報は保持・表示しない
- 列名、タグ名、単価、判定条件、Google DriveフォルダIDは `config/<project>/config.yaml` から読む
- 週次入力データの作成手順は `weekly_input_manual.md` に従う
- 週次分析結果の確認手順は `std_weekly_analysis_guide.md` に従う

## 3. 今回のスコープ
### 必須
- STD Google DriveフォルダIDのconfig読込
- STD週次フォルダからの広告CSV読み取り
- STD週次フォルダからのLステップCSV読み取り
- STD週次フォルダからの全体管理CSV/xlsx読み取り
- CSVの文字コード自動判定
- 列マッピング
- PII破棄
- Lステップ集計
- 広告集計
- 全体管理集計
- 週次説明用の要約出力
- STD週次フォルダへの集計結果出力

### 後回し
- 広告クエリとLステップの厳密な統合アトリビューション
- シナリオ価値スコアの高度化
- RSAの詳細分析
- PDF取り込み
- Google Docs出力
- 自動提案の完全版
- dayvigo対応

## 4. 週次で見る指標
### 広告
- spend
- clicks
- CTR
- CPC
- CVR
- CPA
- ROAS

### Lステップ
- LINE登録数
- LINECVR
- 決済クリック数
- 決済完了数
- 問診票提出率
- 発送済み数
- 要フォロー数
- 商品別件数
- 定期率
- 売上

### 全体管理
- 売上
- 粗利
- 垂直ROAS
- 客単価
- MCPA
- 問診回答数
- 問診遷移率
- LINE/決済CVR
- 商品別CV
- 実CPA

## 5. 週次で説明する観点
- 前週比
- 月累計
- 商品別の伸び
- STD内の商品別・導線別の伸び
- どの層が落ちているか
- 目標に対して不足している量

## 6. ボトルネック判定
### 広告側
- CPA悪化
- ROAS低下
- CTR低下

### 入口側
- LINECVR低下
- 登録数減少

### 決済後
- 決済完了はあるのに問診票提出率が低い
- 要フォロー数が増える

### 売上側
- CVはあるのに売上が伸びない
- 商品ミックスが悪い
- 粗利が目標に届かない

### データ不一致
- 広告CSVと全体管理のCostが大きくズレる
- LステップCSVの決済件数と全体管理のCVが大きくズレる
- 全体管理にしかない数字は全体管理を優先し、差分を注記する

## 7. 最小の出力
### 画面表示
- 週次KPIサマリー
- 月次累計
- 前週比
- ボトルネックの一言要約
- 週次定例でそのまま読める説明メモ
- データ不一致・欠損列・未計測タグの警告

### Google Drive出力
分析対象のSTD週次フォルダ内に `output/` を作り、以下を出力する。

```text
weekly/YYYY-MM-DD/output/
  weekly_dashboard.xlsx
  weekly_report.md
  weekly_kpi.csv
  bottlenecks.csv
  analysis_meta.json
```

| ファイル | 内容 | PII |
| --- | --- | --- |
| `weekly_dashboard.xlsx` | 画面共有用のExcelダッシュボード。KPIカード、商品別表、推移グラフ、構成比、コメントを含む | 含めない |
| `weekly_report.md` | 定例で読める説明メモ。結論、ボトルネック、次アクションを含む | 含めない |
| `weekly_kpi.csv` | 広告、Lステップ、全体管理の主要KPI集計 | 含めない |
| `bottlenecks.csv` | ボトルネック種別、判定理由、根拠指標 | 含めない |
| `analysis_meta.json` | 入力ファイル名、生成日時、警告、集計対象期間 | 含めない |

`weekly_dashboard.xlsx` は、週次定例で画面共有しやすい見た目を優先する。標準シート構成は以下。

| シート | 内容 |
| --- | --- |
| `Dashboard` | KPIカード、今週のコメント、商品別売上・粗利、推移グラフ、商品構成比、ボトルネック要約 |
| `KPI` | 集計値の根拠データ。広告、Lステップ、全体管理の主要KPI |
| `Bottlenecks` | ボトルネック判定の根拠 |
| `Meta` | 入力ファイル名、生成日時、対象期間、警告 |

Dashboardの主な表示項目:

- 売上
- 粗利
- 粗利率
- CV数
- 広告費
- CPA / ROAS
- LINE登録数
- 問診票提出率
- 発送済み数
- 商品別売上・粗利
- 商品構成比
- 週次コメント

## 8. 実装方針
- 既存の `listing-query-tool` の広告分析ロジックは流用する
- 既存の Lステップ分析ロジックは流用する
- 全体管理は週次説明の基準データとして扱う
- 入力元はconfigに設定したSTD Google Driveフォルダとし、フォルダ構成・ファイル名で週を判定する
- Drive API/OAuthで取得したファイルBlobをブラウザ内で解析し、生データは永続化しない
- Driveへの書き戻しは `output/` 配下の集計ファイルのみとし、生CSVや個票は出力しない
- 最初は「統合分析」よりも「週次ボトルネック診断」を優先する
- 途中まで組んである実装はバックアップとして残し、必要に応じて戻せる状態にする

## 9. 受け入れ条件
- configに設定したSTD Google Driveフォルダから、広告CSV、LステップCSV、全体管理CSV/xlsxを読み取れる
- 月曜日に先週分の数字を1画面で見られる
- 週次説明の骨子をそのまま出せる
- STD週次フォルダの `output/` 配下に集計結果が出る
- `weekly_dashboard.xlsx` を開くと、STDの週次状況を1画面で説明できる
- PIIが出ない
- configを変えるだけで列名や単価が変わる

## 10. 非目標
- gclid/utm前提の厳密アトリビューション
- 毎日アラートを出す監視ツール
- 完全自動の最適化提案
- クライアント向けの完成版レポート自動生成
- 認証、課金、マルチテナント
- dayvigo分析

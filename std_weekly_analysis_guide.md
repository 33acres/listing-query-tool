# STD週次分析 運用ガイド

## 1. このシステムの目的
STD（CUREAオンライン性病外来）の週次定例で、先週どこがボトルネックだったかを短時間で説明できる状態にする。

このシステムは、毎日の監視ツールではない。
毎週月曜日に、Google Driveへ置いた3種類のデータを読み込み、広告、Lステップ、全体管理を横並びで確認し、売上・CV・問診・発送までの落ちどころを特定するための週次分析ツールである。

## 2. 現状条件

### 対象
対象はSTDのみ。

dayvigoは今回の要件から外す。

### 入力元
入力元はSTDのGoogle Driveフォルダ。

```text
https://drive.google.com/drive/folders/127os_AXL2Rp49yEUtRKRmyoqh7WfwN11
```

週次フォルダは以下の形式で作る。

```text
weekly/YYYY-MM-DD/
```

`YYYY-MM-DD` は分析を実行する月曜日の日付にする。

例:

```text
weekly/2026-06-23/
```

### 必須入力ファイル
各週次フォルダに以下の3ファイルを置く。

```text
weekly/YYYY-MM-DD/
  ads.csv
  lstep.csv
  management.csv または management.xlsx
```

| ファイル | 内容 |
| --- | --- |
| `ads.csv` | Google広告の検索語句レポート |
| `lstep.csv` | Lステップの友だち情報CSV |
| `management.csv` / `management.xlsx` | 全体管理データ |

### 出力先
分析後、同じ週次フォルダの中に `output/` を作り、集計結果を出す。

```text
weekly/YYYY-MM-DD/output/
  weekly_report.md
  weekly_kpi.csv
  bottlenecks.csv
  analysis_meta.json
```

### 個人情報の扱い
個人情報は保持・表示・出力しない。

出力に含めないもの:

- 氏名
- 表示名
- 電話番号
- メールアドレス
- 住所
- 問診の自由記述
- 友だち1人単位の個票

分析に使うのは集計値のみ。

## 3. ユーザーが毎週やること

### Step 1. 週次フォルダを作る
STD Driveフォルダ内に、分析実行日のフォルダを作る。

例:

```text
weekly/2026-06-23/
```

### Step 2. `ads.csv` を置く
Google広告から検索語句レポートを出力し、`ads.csv` として週次フォルダへアップロードする。

最低限必要な列:

| 用途 | 列名 |
| --- | --- |
| 日付・期間 | `日付` または `月` |
| 検索語句 | `検索語句` |
| 表示回数 | `表示回数` |
| クリック数 | `クリック数` |
| 費用 | `費用` |
| CV数 | `コンバージョン` |

推奨列:

- `キャンペーン`
- `広告グループ`
- `マッチタイプ`
- `クリック率`
- `平均クリック単価`
- `コンバージョン率`
- `コンバージョン単価`

### Step 3. `lstep.csv` を置く
Lステップから友だち情報CSVを出力し、`lstep.csv` として週次フォルダへアップロードする。

必須列:

| 用途 | 列名 |
| --- | --- |
| 集計用ID | `ID` |
| ステータス補助 | `対応マーク` |
| 登録日 | `友だち追加日時` |
| 最終行動日 | `最終アクション日` |
| 申込導線 | `申込経由STEP` |

必須タグ列:

| 分類 | 列名 |
| --- | --- |
| 分岐 | `STEP0_治療希望` |
| 分岐 | `STEP0_検査希望` |
| STEP | `診察誘導_STEP0A` |
| STEP | `診察誘導_STEP1` |
| STEP | `診察誘導_STEP2` |
| STEP | `診察誘導_STEP3` |
| STEP | `診察誘導_STEP4` |
| STEP | `診察誘導_STEP5` |
| STEP | `診察誘導_STEP6` |
| FAQ | `診察誘導_FAQ1` |
| FAQ | `診察誘導_FAQ2` |
| 再アプローチ対象 | `シナ読了_対象` |
| 決済完了 | `STD_決済済み` |
| 問診票提出 | `STD_問診票提出済み` |
| 発送 | `STD_発送済み` |
| 購入商品 | `性感染症治療薬` |
| 購入商品 | `フルセット` |
| 購入商品 | `スタンダードセット` |
| 購入商品 | `ベーシックセット` |
| 購入商品 | `ライトセット` |

### Step 4. `management.csv` または `management.xlsx` を置く
全体管理データをCSVまたはxlsxで出力し、週次フォルダへアップロードする。

全体管理は、最終的な売上・粗利・CV・CPAの基準データとして扱う。

### Step 5. 分析を実行する
ツールで対象週を選び、分析を実行する。

分析実行後、画面表示とDrive出力を確認する。

### Step 6. `output/` を確認する
以下の4ファイルが生成されているか確認する。

```text
output/
  weekly_report.md
  weekly_kpi.csv
  bottlenecks.csv
  analysis_meta.json
```

## 4. 分析結果で確認すること

### 最初に見るもの
まず `weekly_report.md` を見る。

確認する順番:

1. 今週の結論
2. 最大のボトルネック
3. 前週比で悪化した指標
4. 売上・粗利目標に対して不足している量
5. 次に打つべきアクション

週次定例では、このファイルを説明メモの土台にする。

### KPIの確認
`weekly_kpi.csv` で、数字の根拠を確認する。

見るべき指標:

| 領域 | 指標 | 見る理由 |
| --- | --- | --- |
| 広告 | spend | 費用が増えすぎていないか |
| 広告 | clicks | 流入量が落ちていないか |
| 広告 | CTR | 検索語句・広告文が弱くなっていないか |
| 広告 | CPC | クリック単価が高騰していないか |
| 広告 | CVR | 流入後の質が落ちていないか |
| 広告 | CPA | 獲得効率が悪化していないか |
| 広告 | ROAS | 広告費に対して売上が合っているか |
| Lステップ | LINE登録数 | 入口の量が足りているか |
| Lステップ | 決済完了数 | 売上直前のCVが足りているか |
| Lステップ | 問診票提出率 | 決済後に詰まっていないか |
| Lステップ | 発送済み数 | 最終成果まで進んでいるか |
| Lステップ | 要フォロー数 | 放置すべきでない未提出者が増えていないか |
| 全体管理 | 売上 | 最終成果が伸びているか |
| 全体管理 | 粗利 | 売上が伸びても利益が残っているか |
| 全体管理 | 垂直ROAS | 目標ROASに対して足りているか |
| 全体管理 | MCPA | 実質CPAが許容範囲か |

### ボトルネックの確認
`bottlenecks.csv` で、どこが詰まっているかを確認する。

主な判定パターン:

| ボトルネック | 見る指標 | 判断 |
| --- | --- | --- |
| 広告流入不足 | clicks / LINE登録数 | クリックや登録が減っていれば入口不足 |
| 広告効率悪化 | CPA / CVR / ROAS | CPA悪化、CVR低下、ROAS低下なら広告側を確認 |
| LINE遷移不足 | LINECVR | 広告流入はあるがLINE登録が少ないならLP・導線を確認 |
| 決済前離脱 | 決済クリック数 / 決済完了数 | クリックはあるのに決済が少ないなら決済導線を確認 |
| 決済後詰まり | 問診票提出率 / 要フォロー数 | 決済済みなのに問診未提出が多ければ追客を確認 |
| 商品ミックス悪化 | 商品別CV / 客単価 / 粗利 | CVはあるが売上・粗利が弱いなら商品構成を確認 |
| データ不一致 | 広告Cost差分 / CV差分 | 広告、Lステップ、全体管理の集計ズレを確認 |

### 警告の確認
`analysis_meta.json` で、データの信頼性を確認する。

特に見るもの:

- 欠損列がないか
- 読み込めなかったファイルがないか
- 集計対象期間が想定通りか
- Lステップの必須タグ列が揃っているか
- PII列が破棄されたか
- 広告CSVと全体管理のCostが大きくズレていないか
- Lステップの決済件数と全体管理のCVが大きくズレていないか

警告がある場合、数字そのものより先にデータの出し直しを優先する。

## 5. 週次定例での説明順
週次定例では、以下の順で説明する。

1. 今週の結論
2. 売上・粗利・ROASが目標に対してどうか
3. 広告側の変化
4. LINE登録から決済までの変化
5. 決済後の問診票提出・発送の詰まり
6. 商品別の伸び、客単価、粗利への影響
7. 今週やること

説明では、個人別の話はしない。
すべて集計値、割合、前週比、差分で説明する。

## 6. 判断の基本ルール

### 広告費が増えて売上も増えている場合
ROASと粗利を見る。

売上が増えていても、粗利や垂直ROASが悪化していれば良い伸びではない。

### 広告費が同じで売上が落ちている場合
CVR、LINECVR、問診票提出率を順に見る。

どこかの率が落ちていれば、そこが優先改善ポイント。

### CVはあるのに売上が伸びない場合
商品ミックスを見る。

ライトセットや低単価商品に寄っていないか、治療薬と検査キットの比率が変わっていないかを確認する。

### 決済済みなのに問診票提出が少ない場合
決済後フォローを優先する。

`STD_決済済み` があり、`STD_問診票提出済み` がない層が増えている場合は、追客シナリオ、案内文、リマインドタイミングを確認する。

### 数字が合わない場合
全体管理を最終基準にする。

広告CSV、LステップCSV、全体管理の数字がズレる場合は、全体管理を優先し、差分を注記する。

## 7. 完了条件
毎週の運用は、以下が満たされれば完了。

- `ads.csv`、`lstep.csv`、`management.csv` または `management.xlsx` がDriveにある
- 分析が正常に完了している
- 画面に週次サマリーが表示されている
- `output/weekly_report.md` が生成されている
- `output/weekly_kpi.csv` が生成されている
- `output/bottlenecks.csv` が生成されている
- `output/analysis_meta.json` が生成されている
- 出力にPIIが含まれていない
- 週次定例で説明するボトルネックが1つ以上明確になっている

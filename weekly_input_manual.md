# 週次入力データ作成マニュアル

## 目的
毎週月曜日に、先週分のボトルネックを説明するための入力データをGoogle Driveの指定フォルダへそろえる。

対象ファイルは案件ごとに以下の3点。

- `ads.csv`
- `lstep.csv`
- `management.csv` または `management.xlsx`

このマニュアルでは、特に `ads.csv` と `lstep.csv` に何を出せばよいかを定義する。

## 置き場所
ローカルPCではなく、案件ごとのGoogle Drive指定フォルダに置く。

案件フォルダは以下を使う。

| 案件 | Google Driveフォルダ |
| --- | --- |
| dayvigo | https://drive.google.com/drive/folders/1lOKZSbIb13YJKXJ-PZbEAOxS-LeAkell |
| STD | https://drive.google.com/drive/folders/127os_AXL2Rp49yEUtRKRmyoqh7WfwN11 |

各案件フォルダの中で、分析実行日ごとにフォルダを分ける。

例: 2026年6月23日（月）に、前週分を分析する場合。

```text
dayvigo Driveフォルダ/
  weekly/
    2026-06-23/
      ads.csv
      lstep.csv
      management.xlsx

STD Driveフォルダ/
  weekly/
    2026-06-23/
      ads.csv
      lstep.csv
      management.xlsx
```

原則として、集計対象期間は「前週月曜から日曜」。
月次累計も見るため、可能であれば月初から前日までのデータも同じ形式で出せる状態にしておく。

## Google Driveフォルダ運用
ツールは、configに設定された案件別Google Driveフォルダを入力元として読む。

フォルダIDは `config/<project>/config.yaml` の `drive.folder_id` に設定する。

| 案件 | folder_id |
| --- | --- |
| dayvigo | `1lOKZSbIb13YJKXJ-PZbEAOxS-LeAkell` |
| STD | `127os_AXL2Rp49yEUtRKRmyoqh7WfwN11` |

Drive上のファイルはツール側サーバ/DBへ保存しない。
Driveから取得したファイルBlobをブラウザ内で解析し、集計値だけを画面に表示する。

### フォルダ命名ルール
フォルダ名は半角英数字で固定する。

| 階層 | 名前 |
| --- | --- |
| 種別 | `weekly` |
| 分析実行日 | `YYYY-MM-DD` |

例: 2026年6月23日（月）実行分。

```text
weekly/2026-06-23/
```

### ファイル名ルール
各週次フォルダには、以下の3ファイルを必ず置く。

| ファイル名 | 内容 |
| --- | --- |
| `ads.csv` | Google広告の検索語句レポート |
| `lstep.csv` | Lステップの友だち情報CSV |
| `management.csv` または `management.xlsx` | 全体管理データ |

同じフォルダに古い版やバックアップを置かない。
必要な場合は `archive` フォルダを別に作る。

## `ads.csv`

### 何のデータか
Google広告の検索語句レポート。

広告側で知りたいのは、どの検索語句・キャンペーン・広告グループが、費用に対してCVを作れているか。

### 出力元
Google広告の管理画面から、検索語句レポートをCSVで出力する。

既存データの列形式に合わせる場合、以下のようなレポートでよい。

```text
月, 検索語句, マッチタイプ, 追加済み / 除外済み, キャンペーン, 広告グループ,
クリック数, 表示回数, クリック率, 通貨コード, 平均クリック単価, 費用,
キャンペーン タイプ, コンバージョン率, コンバージョン, コンバージョン単価
```

### 必須列
以下の6列は必須。

| 用途 | 推奨列名 | 代替で読める列名 |
| --- | --- | --- |
| 日付・期間 | `日付` または `月` | `日`, `年月日`, `週`, `Date`, `Day`, `Week`, `Month` |
| 検索語句 | `検索語句` | `検索キーワード`, `検索語句テキスト`, `Search term`, `Search keyword` |
| 表示回数 | `表示回数` | `インプレッション`, `インプレッション数`, `Impressions`, `Impr` |
| クリック数 | `クリック数` | `クリック`, `Clicks` |
| 費用 | `費用` | `コスト`, `Cost`, `Cost JPY` |
| CV数 | `コンバージョン` | `コンバージョン数`, `CV`, `Conversions`, `Conv.` |

### 推奨列
必須ではないが、週次説明の精度が上がるため出す。

| 用途 | 推奨列名 |
| --- | --- |
| キャンペーン | `キャンペーン` |
| 広告グループ | `広告グループ` |
| マッチタイプ | `マッチタイプ` |
| CTR | `クリック率` |
| CPC | `平均クリック単価` |
| CVR | `コンバージョン率` |
| CPA | `コンバージョン単価` |
| 売上・CV値 | `コンバージョン値` または `売上` |

### 出力時の注意
- ファイル名は必ず `ads.csv` にする。
- Google Driveの該当週次フォルダへアップロードする。
- 文字コードは `UTF-8` または `Shift_JIS` でよい。
- 合計行、グラフ行、タイトル行は入っていても読み飛ばせるが、可能なら削る。
- `費用` は円表記・カンマ区切りでもよい。
- 指名語、除外候補、マッチタイプは消さずに残す。
- 広告アカウント名や請求情報など、分析に不要な情報は入れない。

## `lstep.csv`

### 何のデータか
Lステップの友だち情報CSV。

Lステップ側で知りたいのは、LINE登録後にどこまで進んだか。

- 登録
- シナリオ到達
- 決済クリック
- 決済完了
- 問診票提出
- 発送
- 商品別購入

### 出力元
Lステップの「友だち情報」または「友だち一覧」からCSVを出力する。

毎週の差分だけではなく、原則として対象案件の友だち全体を出す。
理由は、前週より前に登録した人が、先週に決済・問診票提出・発送へ進むことがあるため。

### 共通の必須列
STD、dayvigo共通で以下を含める。

| 用途 | CSV列名 |
| --- | --- |
| 集計用ID | `ID` |
| ステータス補助 | `対応マーク` |
| 登録日 | `友だち追加日時` |
| 最終行動日 | `最終アクション日` |
| 申込導線 | `申込経由STEP` |

`表示名` はCSVに含まれていてもよいが、ツール側では破棄する。
ただし、可能であれば出力しない。

### 絶対に出さない列
個人情報は分析に使わない。
以下の列は出力しない。

- 氏名
- 表示名以外の名前列
- 電話番号
- メールアドレス
- 住所
- 郵便番号
- 生年月日
- 問診の自由記述
- 診療内容の詳細テキスト

仮にCSVに混ざっても、ツールは config に列挙された列以外を破棄する。
ただし、運用上は最初から出さない方がよい。

### dayvigo: 必須タグ列
CUREAオンライン睡眠外来では、以下のタグ列を含める。

| 分類 | CSV列名 |
| --- | --- |
| STEP | `診察誘導_STEP0A` |
| STEP | `診察誘導_STEP0B` |
| STEP | `診察誘導_STEP1` |
| STEP | `診察誘導_STEP2` |
| STEP | `診察誘導_STEP3` |
| STEP | `診察誘導_STEP4` |
| STEP | `診察誘導_STEP5` |
| STEP | `診察誘導_STEP6` |
| 決済クリック | `RM_決済クリック（単品）` |
| 決済クリック | `RM_決済クリック（定期）` |
| 決済完了 | `決済完了` |
| 決済完了 | `睡眠_決済済み` |
| 問診票提出 | `睡眠_問診票提出済み` |
| 発送 | `睡眠_発送済み` |
| 購入商品 | `デエビゴ定期` |
| 購入商品 | `デエビゴ1ヶ月` |
| 購入商品 | `デエビゴ3ヶ月` |
| 購入商品 | `デエビゴ6ヶ月` |

`睡眠_問診票提出済み` と `睡眠_発送済み` は2026年6月14日以降の運用タグ。
それ以前の期間は、タグが無いことによる未計測が発生する。

`睡眠_診察完了` は現時点ではCSVで出せない前提のため、必須にしない。

### STD: 必須タグ列
CUREAオンライン性病外来では、以下のタグ列を含める。

| 分類 | CSV列名 |
| --- | --- |
| 分岐 | `STEP0_治療希望` |
| 分岐 | `STEP0_検査希望` |
| STEP | `診察誘導_STEP0A` |
| STEP | `診察誘導_STEP1` |
| STEP | `診察誘導_STEP2` |
| STEP | `診察誘導_STEP3` |
| STEP | `診察誘導_STEP4` |
| STEP | `診察誘導_STEP5` |
| STEP | `診察誘導_STEP6` |
| FAQ | `診察誘導_FAQ1` |
| FAQ | `診察誘導_FAQ2` |
| 再アプローチ対象 | `シナ読了_対象` |
| 決済完了 | `STD_決済済み` |
| 問診票提出 | `STD_問診票提出済み` |
| 発送 | `STD_発送済み` |
| 購入商品 | `性感染症治療薬` |
| 購入商品 | `フルセット` |
| 購入商品 | `スタンダードセット` |
| 購入商品 | `ベーシックセット` |
| 購入商品 | `ライトセット` |

`STD_決済済み`、`STD_問診票提出済み`、`STD_発送済み` は2026年6月14日以降の運用タグ。
それ以前の期間は、タグが無いことによる未計測が発生する。

### タグ列の値
Lステップから出力された形式をそのまま使う。

ツール側では以下を「あり」として扱う。

- `1`
- 空欄以外の値
- `true`
- `yes`

以下は「なし」として扱う。

- 空欄
- `0`
- `false`
- `no`

手作業で `1` / `0` に直す必要はない。

### 出力時の注意
- ファイル名は必ず `lstep.csv` にする。
- Google Driveの該当週次フォルダへアップロードする。
- 文字コードは `Shift_JIS` のままでよい。
- Lステップのヘッダー行やタグ列名を手で変更しない。
- Excelで開いて保存し直すと文字化けや日付変換が起きることがあるため、原則そのまま渡す。
- 友だち1人1行のCSVでよいが、ツール画面には個票を表示しない。
- `ID` は集計キーとして使うが、出力画面には表示しない。

## 月曜の作業チェックリスト
1. STDのGoogle Driveフォルダ内に、週次フォルダを作る。
2. dayvigoのGoogle Driveフォルダ内に、週次フォルダを作る。
3. Google広告から検索語句レポートを出し、`ads.csv` として該当フォルダへアップロードする。
4. Lステップから友だち情報CSVを出し、`lstep.csv` として該当フォルダへアップロードする。
5. 全体管理をCSVまたはxlsxで出し、`management.csv` または `management.xlsx` として該当フォルダへアップロードする。
6. `ads.csv` に `日付/検索語句/表示回数/クリック数/費用/コンバージョン` があるか確認する。
7. `lstep.csv` に案件ごとの必須タグ列があるか確認する。
8. 氏名・電話・メール・住所・問診自由記述が入っていないか確認する。
9. ツールで対象案件と対象週を選んで分析する。

## よくあるNG
- `ads.csv` がキャンペーン集計だけで、検索語句列が無い。
- `ads.csv` に費用かコンバージョンが無い。
- `lstep.csv` が前週登録者だけで、過去登録者の先週決済が抜ける。
- Lステップのタグ列名を手で短縮してしまう。
- 週次フォルダに `ads_修正版.csv` など別名ファイルを置く。
- 同じ週次フォルダに古いCSVと新しいCSVを両方置く。
- `表示名`、電話番号、メール、住所を分析画面に出そうとする。
- PDFを主データとして渡す。

PDFは確認資料としては使えるが、週次分析の主入力にはしない。

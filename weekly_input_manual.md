# STD週次入力・出力マニュアル

## 目的
毎週月曜日に、STD（CUREAオンライン性病外来）の先週分ボトルネックを説明するための入力データをGoogle Driveの指定フォルダへそろえる。

対象ファイルは以下の3点。

- `ads.csv`
- `lstep.csv`
- `management.csv` または `management.xlsx`

このマニュアルでは、特に `ads.csv` と `lstep.csv` に何を出せばよいかを定義する。

dayvigoは今回の対象外。

## 全体フロー

```text
Google Driveへ3ファイルをアップロード
  ↓
ツールで対象週を選んで分析を実行
  ↓
画面に週次サマリーを表示
  ↓
Google Driveの output/ に集計結果を書き戻す
```

## 入力の置き場所
ローカルPCではなく、STDのGoogle Drive指定フォルダに置く。

STDフォルダ:
https://drive.google.com/drive/folders/127os_AXL2Rp49yEUtRKRmyoqh7WfwN11

このフォルダの中で、分析実行日ごとにフォルダを分ける。

例: 2026年6月23日（月）に、前週分を分析する場合。

```text
STD Driveフォルダ/
  weekly/
    2026-06-23/
      ads.csv
      lstep.csv
      management.xlsx
      output/
        weekly_dashboard.xlsx
        weekly_report.md
        weekly_kpi.csv
        bottlenecks.csv
        analysis_meta.json
```

原則として、集計対象期間は「前週月曜から日曜」。
月次累計も見るため、可能であれば月初から前日までのデータも同じ形式で出せる状態にしておく。

## Google Driveフォルダ運用
ツールは、configに設定されたSTD Google Driveフォルダを入力元として読む。

フォルダIDは `config/<project>/config.yaml` の `drive.folder_id` に設定する。

```text
config/std/config.yaml
drive.folder_id = 127os_AXL2Rp49yEUtRKRmyoqh7WfwN11
```

Drive上のファイルはツール側サーバ/DBへ保存しない。
Driveから取得したファイルBlobをブラウザ内で解析し、集計値だけを画面に表示する。

### フォルダ命名ルール
フォルダ名は半角英数字で固定する。

| 階層 | 名前 |
| --- | --- |
| 種別 | `weekly` |
| 分析実行日 | `YYYY-MM-DD` |

例: 2026年6月23日（月）実行分。

```text
weekly/2026-06-23/
```

### ファイル名ルール
各週次フォルダには、入力として以下の3ファイルを必ず置く。

| ファイル名 | 内容 |
| --- | --- |
| `ads.csv` | Google広告の検索語句レポート |
| `lstep.csv` | Lステップの友だち情報CSV |
| `management.csv` または `management.xlsx` | 全体管理データ |

同じフォルダに古い版やバックアップを置かない。
必要な場合は `archive` フォルダを別に作る。

## アウトプット

### 画面に出るもの
分析実行後、画面では以下を確認できるようにする。

- 週次KPIサマリー
- 月次累計
- 前週比
- 広告、Lステップ、全体管理の差分
- ボトルネックの一言要約
- 週次定例でそのまま読める説明メモ
- 欠損列、未計測タグ、データ不一致の警告

### Google Driveに出るもの
分析対象の週次フォルダ内に `output/` を作り、以下を出力する。

```text
weekly/YYYY-MM-DD/output/
  weekly_dashboard.xlsx
  weekly_report.md
  weekly_kpi.csv
  bottlenecks.csv
  analysis_meta.json
```

| ファイル | 内容 |
| --- | --- |
| `weekly_dashboard.xlsx` | 週次定例で画面共有するExcelダッシュボード |
| `weekly_report.md` | 定例で読める説明メモ。結論、ボトルネック、次アクション |
| `weekly_kpi.csv` | 広告、Lステップ、全体管理の主要KPI集計 |
| `bottlenecks.csv` | ボトルネック種別、判定理由、根拠指標 |
| `analysis_meta.json` | 入力ファイル名、生成日時、警告、集計対象期間 |

出力ファイルには、氏名・電話・メール・住所・問診自由記述などの個人情報を含めない。

### `weekly_dashboard.xlsx` の見た目
Excelダッシュボードは、画面共有しやすい1枚目の `Dashboard` シートを中心にする。

- 上部: タイトル、対象期間、最終更新日時
- KPIカード: 売上、粗利、粗利率、CV数、広告費、CPA、ROAS
- コメント枠: 今週の結論と最大ボトルネック
- 表: 商品別の売上、CV、構成比、粗利
- グラフ: 週次または日次の売上・粗利推移
- グラフ: 商品構成比
- 補足: 警告、未計測、データ不一致

対象はSTDのみなので、商品カテゴリは以下を基本にする。

- 性感染症治療薬
- フルセット
- スタンダードセット
- ベーシックセット
- ライトセット

## `ads.csv`

### 何のデータか
Google広告の検索語句レポート。

広告側で知りたいのは、どの検索語句・キャンペーン・広告グループが、費用に対してCVを作れているか。

### 出力元
Google広告の管理画面から、検索語句レポートをCSVで出力する。

既存データの列形式に合わせる場合、以下のようなレポートでよい。

```text
月, 検索語句, マッチタイプ, 追加済み / 除外済み, キャンペーン, 広告グループ,
クリック数, 表示回数, クリック率, 通貨コード, 平均クリック単価, 費用,
キャンペーン タイプ, コンバージョン率, コンバージョン, コンバージョン単価
```

### 必須列
以下の6列は必須。

| 用途 | 推奨列名 | 代替で読める列名 |
| --- | --- | --- |
| 日付・期間 | `日付` または `月` | `日`, `年月日`, `週`, `Date`, `Day`, `Week`, `Month` |
| 検索語句 | `検索語句` | `検索キーワード`, `検索語句テキスト`, `Search term`, `Search keyword` |
| 表示回数 | `表示回数` | `インプレッション`, `インプレッション数`, `Impressions`, `Impr` |
| クリック数 | `クリック数` | `クリック`, `Clicks` |
| 費用 | `費用` | `コスト`, `Cost`, `Cost JPY` |
| CV数 | `コンバージョン` | `コンバージョン数`, `CV`, `Conversions`, `Conv.` |

### 推奨列
必須ではないが、週次説明の精度が上がるため出す。

| 用途 | 推奨列名 |
| --- | --- |
| キャンペーン | `キャンペーン` |
| 広告グループ | `広告グループ` |
| マッチタイプ | `マッチタイプ` |
| CTR | `クリック率` |
| CPC | `平均クリック単価` |
| CVR | `コンバージョン率` |
| CPA | `コンバージョン単価` |
| 売上・CV値 | `コンバージョン値` または `売上` |

### 出力時の注意
- ファイル名は必ず `ads.csv` にする。
- Google Driveの該当週次フォルダへアップロードする。
- 文字コードは `UTF-8` または `Shift_JIS` でよい。
- 合計行、グラフ行、タイトル行は入っていても読み飛ばせるが、可能なら削る。
- `費用` は円表記・カンマ区切りでもよい。
- 指名語、除外候補、マッチタイプは消さずに残す。
- 広告アカウント名や請求情報など、分析に不要な情報は入れない。

## `lstep.csv`

### 何のデータか
Lステップの友だち情報CSV。

Lステップ側で知りたいのは、LINE登録後にどこまで進んだか。

- 登録
- シナリオ到達
- 決済クリック
- 決済完了
- 問診票提出
- 発送
- 商品別購入

### 出力元
Lステップの「友だち情報」または「友だち一覧」からCSVを出力する。

毎週の差分だけではなく、原則として対象案件の友だち全体を出す。
理由は、前週より前に登録した人が、先週に決済・問診票提出・発送へ進むことがあるため。

### 必須列
以下を含める。

| 用途 | CSV列名 |
| --- | --- |
| 集計用ID | `ID` |
| ステータス補助 | `対応マーク` |
| 登録日 | `友だち追加日時` |
| 最終行動日 | `最終アクション日` |
| 申込導線 | `申込経由STEP` |

`表示名` はCSVに含まれていてもよいが、ツール側では破棄する。
ただし、可能であれば出力しない。

### 絶対に出さない列
個人情報は分析に使わない。
以下の列は出力しない。

- 氏名
- 表示名以外の名前列
- 電話番号
- メールアドレス
- 住所
- 郵便番号
- 生年月日
- 問診の自由記述
- 診療内容の詳細テキスト

仮にCSVに混ざっても、ツールは config に列挙された列以外を破棄する。
ただし、運用上は最初から出さない方がよい。

### 必須タグ列
CUREAオンライン性病外来では、以下のタグ列を含める。

| 分類 | CSV列名 |
| --- | --- |
| 分岐 | `STEP0_治療希望` |
| 分岐 | `STEP0_検査希望` |
| STEP | `診察誘導_STEP0A` |
| STEP | `診察誘導_STEP1` |
| STEP | `診察誘導_STEP2` |
| STEP | `診察誘導_STEP3` |
| STEP | `診察誘導_STEP4` |
| STEP | `診察誘導_STEP5` |
| STEP | `診察誘導_STEP6` |
| FAQ | `診察誘導_FAQ1` |
| FAQ | `診察誘導_FAQ2` |
| 再アプローチ対象 | `シナ読了_対象` |
| 決済完了 | `STD_決済済み` |
| 問診票提出 | `STD_問診票提出済み` |
| 発送 | `STD_発送済み` |
| 購入商品 | `性感染症治療薬` |
| 購入商品 | `フルセット` |
| 購入商品 | `スタンダードセット` |
| 購入商品 | `ベーシックセット` |
| 購入商品 | `ライトセット` |

`STD_決済済み`、`STD_問診票提出済み`、`STD_発送済み` は2026年6月14日以降の運用タグ。
それ以前の期間は、タグが無いことによる未計測が発生する。

### タグ列の値
Lステップから出力された形式をそのまま使う。

ツール側では以下を「あり」として扱う。

- `1`
- 空欄以外の値
- `true`
- `yes`

以下は「なし」として扱う。

- 空欄
- `0`
- `false`
- `no`

手作業で `1` / `0` に直す必要はない。

### 出力時の注意
- ファイル名は必ず `lstep.csv` にする。
- Google Driveの該当週次フォルダへアップロードする。
- 文字コードは `Shift_JIS` のままでよい。
- Lステップのヘッダー行やタグ列名を手で変更しない。
- Excelで開いて保存し直すと文字化けや日付変換が起きることがあるため、原則そのまま渡す。
- 友だち1人1行のCSVでよいが、ツール画面には個票を表示しない。
- `ID` は集計キーとして使うが、出力画面には表示しない。

## 月曜の作業チェックリスト
1. STDのGoogle Driveフォルダ内に、週次フォルダを作る。
2. Google広告から検索語句レポートを出し、`ads.csv` として該当フォルダへアップロードする。
3. Lステップから友だち情報CSVを出し、`lstep.csv` として該当フォルダへアップロードする。
4. 全体管理をCSVまたはxlsxで出し、`management.csv` または `management.xlsx` として該当フォルダへアップロードする。
5. `ads.csv` に `日付/検索語句/表示回数/クリック数/費用/コンバージョン` があるか確認する。
6. `lstep.csv` にSTDの必須タグ列があるか確認する。
7. 氏名・電話・メール・住所・問診自由記述が入っていないか確認する。
8. ツールで対象週を選んで分析する。
9. 画面の週次サマリーを確認する。
10. Driveの `output/` に5つの出力ファイルができているか確認する。
11. `weekly_dashboard.xlsx` を開き、画面共有できる状態か確認する。

## よくあるNG
- `ads.csv` がキャンペーン集計だけで、検索語句列が無い。
- `ads.csv` に費用かコンバージョンが無い。
- `lstep.csv` が前週登録者だけで、過去登録者の先週決済が抜ける。
- Lステップのタグ列名を手で短縮してしまう。
- 週次フォルダに `ads_修正版.csv` など別名ファイルを置く。
- 同じ週次フォルダに古いCSVと新しいCSVを両方置く。
- `表示名`、電話番号、メール、住所を分析画面に出そうとする。
- PDFを主データとして渡す。

PDFは確認資料としては使えるが、週次分析の主入力にはしない。
